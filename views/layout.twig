<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% block title %} {{ app_name}} {% endblock %}</title>

    {# Bootstrap 5 CDN + Icons #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    {% block css %}{% endblock %}

    {# Custom CSS #}
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="#">Vacation Portal</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            {% if auth_user %}
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    {% if auth_user.role == 'manager' %}
                        <li class="nav-item">
                            <a class="nav-link {% if app.request.path starts with '/users' %}active{% endif %}" href="/users">
                                <i class="bi bi-people"></i> Users
                            </a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.path == '/requests' %}active{% endif %}" href="/requests">
                            <i class="bi bi-calendar-check"></i> Requests
                            {% if auth_user.role == 'manager' and totalPendingRequests > 0 %}
                                <span class="badge bg-warning"> {{ totalPendingRequests }}</span>
                            {% endif %}

                        </a>
                    </li>
                </ul>
            {% endif %}

            <ul class="navbar-nav ms-auto">
                {% if auth_user %}
                    <li class="nav-item">
                        <form action="/logout" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-outline-danger">Logout</button>
                        </form>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<main class="container">
    {% block content %}{% endblock %}
</main>

<footer class="text-center text-muted py-3 border-top">
    <small>&copy; {{ "now"|date("Y") }} {{ app_name }}</small>
</footer>

{% block scripts %}{% endblock %}

</body>
</html>
